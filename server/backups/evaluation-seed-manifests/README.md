# Evaluation seed manifests (local operator output)

Manifest JSON files for `AGRITRACK_EVAL_DATASET_v1` are written here after a
successful Evaluation seed. They contain planting/activity/harvest IDs only —
never credentials.

Do not treat this folder as application runtime data.
